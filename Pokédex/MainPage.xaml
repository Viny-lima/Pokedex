<Page
    x:Class="Pokédex.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pokédex"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" >
            <TextBox    Name="Id"
                        Width="400"
                        Margin="20"
                        Padding="10"                   
                        />
            <Button Content="FIND" Click="Button_Click" Margin="20" Background="Green" Foreground="White"/>
        </StackPanel>

        <TextBlock Name="NameId" Grid.Row="1" FontSize="30" HorizontalAlignment="Center" Margin="10"/>
        
        <StackPanel Grid.Row="2" Width="500">
            <Image Name="imgPokemom" Source="{x:Bind local:MainPage.IdPokemom,  Mode=OneTime}"/>
        </StackPanel>
        

    </Grid>
</Page>
