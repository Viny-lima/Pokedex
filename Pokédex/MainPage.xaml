<Page
    x:Class="Pokédex.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pokédex"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:data="using:Pokédex.Models"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Name="MyGrid">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" >
            <TextBox Name="IdPokemon"
                     Width="300"
                     Margin="30"
                     Padding="10" TextChanged="IdPokemon_TextChanged"                   
                     />
            <Button Content="FIND" Margin="20" Background="Green" Foreground="White" Click="ChangePokemon"/>






        </StackPanel>

        <TextBlock Name="TestURl" Grid.Row="1"/>

        <Image Width="500" Grid.Row="2" >
            <Image.Source>
                <BitmapImage UriSource="{x:Bind pokemon.Sprite, Mode=OneWay}" />
            </Image.Source>
        </Image>
        <Button Content="Button" Margin="292,170,0,0" Background="DarkViolet" Grid.Row="2" VerticalAlignment="Top"/>




    </Grid>
</Page>

    